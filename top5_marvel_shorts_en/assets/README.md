# Placeholder Assets

The current `.svg` files are placeholders. Replace them with final files using
the same names, or update `index.html` and `manifest.json` if you want PNG/JPG
filenames instead.

Expected filenames are listed in `manifest.json`.
